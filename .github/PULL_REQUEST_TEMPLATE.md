## What does this PR change?

## Why is it needed?

## Verification

- [ ] `npm run typecheck` passes
- [ ] `npm test` passes
- [ ] `dist/index.js` rebuilt (`npm run bundle`) and committed, if `src/` changed
- [ ] Docs updated (`README.md` / `docs/` / `.swarm.yml` comments), if behavior or config changed

## Risk domains (for reviewer focus)

<!-- e.g. providers, budget, debate, principal synthesis, static analysis, docs -->
